/*
  Exercício: Classe Produto com tipos específicos
  Nível: Médio
  Tópico: Herança

  Implemente a classe `Produto` com `nome` e `preco`. Crie as subclasses `Livro`
  (com `autor`) e `Eletronico` (com `voltagem`). Use o `super` para reaproveitar o
  construtor base.
*/

// Escreva sua solução aqui
