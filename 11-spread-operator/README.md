# Spread Operator


## ✅ Nível Fácil

1. **Copiar um array usando spread** — Dado um array, crie uma cópia dele usando spread operator.
2. **Concatenar dois arrays com spread** — Dado dois arrays, crie um novo array que junte os dois usando spread.
3. **Adicionar elementos a um array existente** — Crie um novo array adicionando novos elementos no começo e no final de um array já existente usando spread.
4. **Clonar um objeto simples usando spread** — Dado um objeto, crie uma cópia dele usando spread operator.

## 🟡 Nível Médio

1. **Mesclar dois objetos em um novo objeto** — Dado dois objetos com algumas propriedades, crie um objeto novo que contenha as propriedades de ambos usando spread.
2. **Adicionar ou sobrescrever propriedades em um objeto usando spread** — Dado um objeto, crie uma cópia dele adicionando ou alterando propriedades específicas com spread.
3. **Passar elementos de um array como argumentos em uma função** — Use spread para passar elementos de um array como argumentos separados para uma função.
4. **Clonar um array aninhado parcialmente usando spread** — Dado um array que contém outros arrays, crie uma cópia superficial do array principal usando spread, mas mantendo os arrays internos referenciados.

## 🔴 Nível Difícil

1. **Mesclar múltiplos objetos com conflito de propriedades** — Dado três ou mais objetos com algumas propriedades em comum, mescle-os usando spread e analise qual valor fica no resultado final.
2. **Atualizar propriedades profundas de objeto com spread** — Dado um objeto com propriedades aninhadas, crie uma nova versão do objeto onde uma propriedade interna é atualizada, usando spread para preservar imutabilidade.
3. **Criar uma função que recebe múltiplos arrays e retorna um array concatenado sem duplicatas usando spread** — Receba vários arrays, una-os em um único array eliminando valores duplicados usando spread.
4. **Usar spread para clonar objetos e arrays dentro de um estado React** — Explique como usar spread para atualizar estados imutáveis em React, clonando objetos e arrays corretamente, e aplique em um cenário hipotético.
