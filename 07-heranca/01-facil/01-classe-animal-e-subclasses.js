/*
  Exercício: Classe Animal e subclasses
  Nível: Fácil
  Tópico: Herança

  Crie uma classe `Animal` com propriedades `nome` e `idade`. Depois, crie duas
  subclasses: `Cachorro` e `Gato`, que herdam de `Animal`. Instancie objetos das
  subclasses e exiba as propriedades herdadas.
*/

// Escreva sua solução aqui
